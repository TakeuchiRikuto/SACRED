# For pip installation after conda setup
# Use this AFTER running: conda install pytorch rdkit numpy pandas scipy scikit-learn matplotlib seaborn -c pytorch -c conda-forge

# Language model dependencies
transformers>=4.30.0

# Molecular processing
selfies>=2.1.1

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# PyTorch Geometric (install with correct torch version)
# Detect version: python -c "import torch; print(torch.__version__)"
# Then install:
# pip install torch-scatter torch-sparse torch-cluster torch-spline-conv torch-geometric -f https://data.pyg.org/whl/torch-{VERSION}+cpu.html

# Optional advanced features
# wandb>=0.15.0  # Experiment tracking
# jupyterlab>=4.0.0  # Notebook interface
# ipywidgets>=8.0.0  # Interactive widgets