# Core dependencies
torch>=2.0.0
transformers>=4.30.0

# PyTorch Geometric (install separately with specific torch version)
# pip install torch-scatter torch-sparse torch-cluster torch-spline-conv torch-geometric -f https://data.pyg.org/whl/torch-2.0.0+cpu.html

# Molecular processing
rdkit  # Install via conda: conda install -c conda-forge rdkit
# or pip install rdkit-pypi
selfies>=2.1.1

# Scientific computing  
numpy>=1.24.0,<2.0.0  # Compatibility with older packages
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
# logging is built-in, no need to install

# Optional for advanced features
# wandb>=0.15.0  # Uncomment if needed
# jupyterlab>=4.0.0  # Uncomment if needed